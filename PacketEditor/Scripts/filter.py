#data = data # data contain the request/response
#monitor = 1 # 0=hidden, 1=displayd
#monitor_color = 0 # 0=black, 1=dark_green, 2=red


def filter(monitor, monitor_color, data):
  #import re
  monitor_color=2

  print("[Filter]")
  return [monitor, monitor_color,data]